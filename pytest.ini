[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = tests
addopts = -ra --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    ai: yapay zekâ sağlayıcısı gerektiren testler
    slow: uzun süren testler
