# Akıllı Restaurant Yönetim Sistemi - çalışma zamanı bağımlılıkları
# Python 3.11+ (3.12 önerilir)
#
# Sürümler `pip-audit` ile taranmıştır; bilinen güvenlik açığı bulunan
# sürümler kullanılmaz. Güncelleme sonrası tekrar tarayın:
#     python -m pip_audit

# --- Çekirdek ---
Django==5.2.16
djangorestframework==3.18.0
django-filter==24.3
django-cors-headers==4.9.0
djangorestframework-simplejwt==5.5.1

# --- Gerçek zamanlı (mutfak ekranı / canlı sipariş) ---
channels==4.3.2
daphne==4.2.3
twisted==26.4.0

# --- Yapılandırma ve güvenlik ---
python-dotenv==1.2.2
cryptography>=46.0.4
pyOpenSSL>=26.0.0
django-axes==7.0.0
whitenoise==6.12.0

# --- Görsel / dosya ---
pillow==12.3.0
qrcode==8.2

# --- Raporlama ve dışa aktarma ---
openpyxl==3.1.5
reportlab==4.2.5

# --- HTTP istemcisi (AI sağlayıcıları) ---
httpx==0.28.1
